#Challenge 1 (Make a change to the first line of the file and push it)
#Using #1's repo collaboratively 
#(2) make a change to the first line of a file and push it
#(1) make a change to the last line
#(1) pull the repo (the changes should be merged automatically)
#(1) push the repo
#(2) pull the repo
#Both collaborators now have identical copies of #1’s repo - change to the last line
